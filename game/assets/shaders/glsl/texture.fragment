#version 100

varying vec2 uv;

uniform sampler2D TEXTURE_0;

void main()
{
	vec4 color = texture2D( TEXTURE_0, uv );

#ifdef ALPHA_TEST
	if(color.a < 0.5)
		discard;
#endif

	gl_FragColor = color;
}
